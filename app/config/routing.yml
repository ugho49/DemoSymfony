app:
    resource: '@AppBundle/Controller/'
    type: annotation
    prefix: /

api:
    resource: "@ApiBundle/Controller/"
    type:     annotation
    prefix:   /api

admin:
    resource: "@AdminBundle/Controller/"
    type:     annotation
    prefix:   /admin

logout:
    path: /logout
